{
  "translate_provider_id": {
    "description": "指定用于翻译的 LLM 提供商 ID（可选）",
    "type": "string",
    "hint": "留空则使用当前会话默认的提供商。填写后会调用 context.get_provider_by_id(provider_id) 指定的提供商进行翻译。仅在指令带 translate 参数时生效。",
    "default": ""
  },
  "cache_ttl_seconds": {
    "description": "主数据（cards / cardEpisodes / gameCharacters）缓存时长（秒）",
    "type": "int",
    "hint": "避免每次指令都重新下载全量主数据。默认 3600 秒，即 1 小时。",
    "default": 3600
  },
  "http_timeout_seconds": {
    "description": "单次 HTTP 请求超时时间（秒）",
    "type": "int",
    "default": 30
  }
}
